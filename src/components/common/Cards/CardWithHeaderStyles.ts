import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  boxWithHeader: {},
  appBar: {
    color: "#fff",
    padding: "0px 10px",
    borderRadius: "10px 10px 0px 0px",
  },
});
