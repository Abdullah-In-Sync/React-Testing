import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => {
  return {
    tableWrapper: {
      "& .isActive_0": {
        color: "red",
      },
    },
  };
});
